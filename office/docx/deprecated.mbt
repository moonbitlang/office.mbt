///|
#deprecated("implicit trait-method promotion is being removed; call via the trait")
pub extend DocxAnnotationParseError with @moonbitlang/core/debug.Debug::{
  to_repr,
}

///|
#deprecated("implicit trait-method promotion is being removed; call via the trait")
pub extend DocxAnnotationOpResult with Eq::{not_equal, equal}

///|
#deprecated("implicit trait-method promotion is being removed; call via the trait")
pub extend DocxAnnotationOpResult with @moonbitlang/core/debug.Debug::{to_repr}

///|
#deprecated("implicit trait-method promotion is being removed; call via the trait")
pub extend DocxAnnotationReport with Eq::{not_equal, equal}

///|
#deprecated("implicit trait-method promotion is being removed; call via the trait")
pub extend DocxAnnotationReport with @moonbitlang/core/debug.Debug::{to_repr}

///|
#deprecated("implicit trait-method promotion is being removed; call via the trait")
pub extend DocxAuthorParseError with @moonbitlang/core/debug.Debug::{to_repr}

///|
#deprecated("implicit trait-method promotion is being removed; call via the trait")
pub extend DocxAuthorReport with Eq::{not_equal, equal}

///|
#deprecated("implicit trait-method promotion is being removed; call via the trait")
pub extend DocxAuthorReport with @moonbitlang/core/debug.Debug::{to_repr}

///|
#deprecated("implicit trait-method promotion is being removed; call via the trait")
pub extend DocxTemplateFinding with Eq::{not_equal, equal}

///|
#deprecated("implicit trait-method promotion is being removed; call via the trait")
pub extend DocxTemplateFinding with @moonbitlang/core/debug.Debug::{to_repr}

///|
#deprecated("implicit trait-method promotion is being removed; call via the trait")
pub extend DocxTemplateReport with Eq::{not_equal, equal}

///|
#deprecated("implicit trait-method promotion is being removed; call via the trait")
pub extend DocxTemplateReport with @moonbitlang/core/debug.Debug::{to_repr}

///|
#deprecated("implicit trait-method promotion is being removed; call via the trait")
pub extend DocxTemplateRefused with @moonbitlang/core/debug.Debug::{to_repr}

///|
#deprecated("implicit trait-method promotion is being removed; call via the trait")
pub extend DocxRegionOutcome with Eq::{not_equal, equal}

///|
#deprecated("implicit trait-method promotion is being removed; call via the trait")
pub extend DocxRegionOutcome with @moonbitlang/core/debug.Debug::{to_repr}
