# Office CLI

Run the published XLSX and DOCX command through Mooncakes:

```sh
moonx bobzhang/office help all --json
moonx bobzhang/office <command> <args...>
```

Arguments after the package coordinate are passed directly to `office`; an
explicit `--` separator is not required.
