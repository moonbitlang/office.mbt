Vendored Generic Polygon Clipper sources from CamlPDF's `gpc.c` and `gpc.h`.

The upstream files carry Alan Murta's MIT-style license header. They are kept
unchanged so the native `pdf_clip` binding can build without relying on any
local migration source checkout.
