Vendored miniz 3.0.0 source used by `pdf_zlib_native.c`.

Source: CamlPDF's vendored `miniz.c` and `miniz.h` reference copies.
License: see the copyright and unlicense notices in `miniz.c` and `miniz.h`.
