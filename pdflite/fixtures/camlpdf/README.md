CamlPDF-derived PDF fixtures used by acceptance tests.

These files are checked into the project so tests do not depend on a
reference-only local checkout:

- `logo.pdf`
- `introduction_to_camlpdf.pdf`
