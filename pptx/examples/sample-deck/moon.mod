// Modified for office.mbt: official module namespace and workspace integration.
name = "moonbitlang/pptx-sample-deck"

version = "0.0.1"

description = "Runnable sample deck for moon-pptx — demonstrates every feature across several decks. Doubles as a debugging helper for PowerPoint Online compatibility verification."

license = "Apache-2.0"

import {
  "moonbitlang/pptx@0.1.0",
}

source = "main"
