// Modified for office.mbt: official module namespace and workspace integration.
name = "moonbitlang/pptx-bench"

version = "0.0.1"

description = "Benchmark harness for moon-pptx — the moon-pptx side of the cross-library comparison in ROADMAP.md §3.2."

license = "Apache-2.0"

import {
  "moonbitlang/pptx@0.1.0",
}

source = "main"
